import React from "react";
import classes from "./Window.css";

function Window(props) {
  return (
    <section>
      <h1>{props.header||" "}</h1>
        <ul>
            
        </ul>
    </section>
  );
}

export default Window;
